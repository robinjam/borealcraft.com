<h1><%= @page.title %></h1>

<p id="notice"><%= notice %></p>

<article>
  <%= raw @page.content %>
</article>

<p>
  <%= link_to 'Edit', edit_page_path(@page) %> |
  <%= link_to 'Delete', delete_page_path(@page) %>
</p>
