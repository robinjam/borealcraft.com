<h1><%= @page.title %></h1>

<article>
  <%= markdown @page.content %>
  <% if admin? %>
    <p data-admin-tool>
      <%= link_to 'Edit', edit_page_path(@page) %> |
      <%= link_to 'Delete', delete_page_path(@page) %>
    </p>
  <% end %>
</article>
