<%= form_for(@headline) do |f| %>
  <% if @headline.errors.any? %>
    <p id="error_explanation">
      <h2><%= pluralize(@headline.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% @headline.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </p>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %>
    <%= f.text_area :content, style: 'width: 100%;' %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
